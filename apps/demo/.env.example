# Iris demo dashboard configuration

# WebSocket URL of the local Iris bridge the SDK connects to in dev.
VITE_IRIS_WS_URL=ws://localhost:4400/iris

# Pairing token — must match IRIS_TOKEN in the @iris/server .env (blank for localhost dev).
VITE_IRIS_TOKEN=
