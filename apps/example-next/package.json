{
  "name": "@reticle/example-next",
  "private": true,
  "version": "1.1.0",
  "description": "Minimal Next.js (App Router) app — verifies Reticle via withReticle + a dev-only client connect.",
  "scripts": {
    "dev": "next dev --webpack -p 5302"
  },
  "dependencies": {
    "@reticle/core": "workspace:*",
    "next": "^16.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
