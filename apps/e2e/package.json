{
  "name": "@reticle/e2e",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "description": "Committed end-to-end battery: boots api+demo+next-smoke and drives Reticle in a real (headless) browser. The product's regression safety net.",
  "scripts": {
    "e2e": "node run.mjs",
    "e2e:ci": "bash run-ci.sh"
  },
  "dependencies": {
    "@reticle/server": "workspace:*",
    "@reticle/test": "workspace:*",
    "playwright": "^1.48.0"
  }
}
