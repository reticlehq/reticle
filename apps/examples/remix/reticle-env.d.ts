// Injected by vite.config's `define` from the daemon's ~/.reticle/pairing-token (a JSON string literal;
// declared here — not inline — so `define`'s textual replacement can't corrupt the declaration).
declare const __RETICLE_TOKEN__: string;
