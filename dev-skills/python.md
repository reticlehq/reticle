# dev-skills/python.md — N/A for this stack

Iris is a pure-TypeScript monorepo. There is no Python service.

This file is kept for parity with the Project Foundation skill. If a Python component is
ever added (e.g. an ML-based snapshot ranker), populate this file from Foundation II.11 and
II.1–II.3 and add `pyproject.toml` with `mypy strict = true`,
`disallow_any_explicit = true`, and ruff. Until then: **N/A**.

Equivalent rules already enforced for our TS code live in `skills/typescript.md`.
