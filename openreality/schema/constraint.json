{
  "$id": "https://openreality.dev/schema/v1/constraint.json",
  "$ref": "#/definitions/constraint",
  "definitions": {
    "constraint": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "statement": {
          "type": "string",
          "minLength": 1
        },
        "predicate": {},
        "channels": {
          "type": "array",
          "items": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "ui",
                  "net",
                  "state",
                  "signal",
                  "log",
                  "route",
                  "storage",
                  "time",
                  "visual"
                ]
              },
              {
                "type": "string",
                "pattern": "^x-[a-z0-9-]+$"
              }
            ]
          },
          "minItems": 1
        },
        "severity": {
          "type": "string",
          "enum": ["blocking", "advisory"],
          "default": "blocking"
        }
      },
      "required": ["id", "statement", "channels"],
      "additionalProperties": false
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}
