### Changed

- **`@reticlehq/browser` — the chat panel's cards live in one carousel at the top of the log.** "Get Harness free for 3 months" (only when the offer applies to your workspace) and "Talk to Founders" share one carousel with dots to switch between them and a single close control that removes it for the tab session. It sits inside the log's scroll area, so the panel, the log and the toolbar keep their exact sizes, and new log lines push it up and out of view. Before, the two cards sat above the log and took height from it.
